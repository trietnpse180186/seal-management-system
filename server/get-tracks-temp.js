const mongoose = require('mongoose'); mongoose.connect('mongodb://127.0.0.1/seal-hackathon').then(async () => { const Track = mongoose.model('Track', new mongoose.Schema({}), 'tracks'); const tracks = await Track.find({}); console.log(tracks.map(t => ({name: t.get('name'), eventId: t.get('eventId')}))); process.exit(0); }); ; node server/get-tracks-temp.js; rm server/get-tracks-temp.js
